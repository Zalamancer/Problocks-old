{
  "name": "@problocks/sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Problocks SDK — the API surface available to simulation developers",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest"
  },
  "dependencies": {
    "@problocks/engine": "0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}

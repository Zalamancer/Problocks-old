# Problocks — Claude Code Instructions

## Dev Server

When `npm run dev` needs to run (testing changes, viewing the app, verifying fixes), run it automatically without asking the user. Kill any existing dev server on port 4000/4001 first, then start fresh.
